# Optional environment overrides for comfyui-windows-installer
# Copy to .env (same folder) and edit as needed. All values are optional.

# Launcher
# Quiet mode for launcher prints (1=quiet, 0=show env summary). Process still runs in foreground.
# HEADLESS=0
# Name of the Conda environment (default: ComfyUI)
# COMFYUI_ENV_NAME=ComfyUI

# Paths
# Absolute path to your ComfyUI checkout (default: %USERPROFILE%\ComfyUI)
# COMFYUI_DIR=C:\Users\YOURNAME\ComfyUI
# Base path for models if you keep them on a different drive/NAS
# MODEL_BASE_PATH=Z:\AI\Models
# Optional IO directories
# INPUT_DIR=C:\Users\YOURNAME\Pictures\_input
# OUTPUT_DIR=C:\Users\YOURNAME\Pictures\_output
# TEMP_DIR=C:\Users\YOURNAME\Pictures\_temp

# Server
# Port for the web UI (default: 8188)
# SERVER_PORT=8188
# Extra CLI flags for ComfyUI main.py (space-separated)
# CUSTOM_PARAMETERS=--some-flag --another=value

# Watcher tuning (advanced)
# Enable restart when there is no output for a long time and CPU is quiet
# ENABLE_NO_OUTPUT_RESTART=1
# Seconds of no output before considering restart
# NO_OUTPUT_RESTART_SECS=600
# CPU percentage threshold to consider "quiet"
# QUIET_CPU_THRESHOLD=2.0
# Seconds CPU must remain below threshold before restart is allowed
# QUIET_CPU_WINDOW_SECS=300
# Seconds to wait for startup readiness logs (we keep monitoring even if logs are quiet)
# BOOT_WAIT_TIME=1600
# Interval between monitor checks (seconds)
# MONITOR_INTERVAL=10
